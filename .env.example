# Optional Worker API origin, for push alerts and LLM copy endpoints.
# Example: https://realtemp-api.<account>.workers.dev
VITE_API_BASE=

# Optional Sentry DSN. When set, RealTemp initializes crash reporting at startup.
# Keep the real value in .env or Vercel environment variables, not in git.
VITE_SENTRY_DSN=
